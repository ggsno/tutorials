type Movie {
  id: Int!
  title: String!
  rating: Float!
  summary: String
  medium_cover_image: String
  background_image: String
}

type Query {
  getMovies: [Movie]!
  getById(id: Int!): Movie
}

